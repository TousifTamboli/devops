version: "3.9"

services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile.fastapi
    ports:
      - "8000:8000"

  flask:
    build:
      context: .
      dockerfile: Dockerfile.flask
    ports:
      - "5001:5000"
    depends_on:
      - fastapi